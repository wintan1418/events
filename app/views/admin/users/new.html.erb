<% content_for(:page_title) { "New User" } %>

<div class="max-w-2xl">
  <div class="mb-6">
    <%= link_to "Back", admin_users_path, class: "text-sm text-gray-400 hover:text-white" %>
  </div>
  <h1 class="text-2xl font-bold text-white mb-6">New User</h1>
  <%= render "form", user: @user %>
</div>
