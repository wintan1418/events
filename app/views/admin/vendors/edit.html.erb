<% content_for(:page_title) { "Edit #{@vendor.name}" } %>

<div class="max-w-2xl">
  <div class="mb-6">
    <%= link_to "Back", admin_vendors_path, class: "text-sm text-gray-400 hover:text-white" %>
  </div>
  <h1 class="text-2xl font-bold text-white mb-6">Edit Vendor</h1>
  <%= render "form", vendor: @vendor %>
</div>
